import BaseNodeIrys from "../base.js";
import ArweaveConfig from "../tokens/arweave.js";
export class ArweaveIrys extends BaseNodeIrys {
    constructor({ url, key, config }) {
        super({
            url,
            config,
            getTokenConfig: (irys) => new ArweaveConfig({
                irys,
                name: "arweave",
                ticker: "AR",
                minConfirm: 10,
                providerUrl: config?.providerUrl ?? "https://arweave.net",
                wallet: key,
                isSlow: true,
                opts: config?.tokenOpts,
            }),
        });
    }
}
//# sourceMappingURL=arweave.js.map