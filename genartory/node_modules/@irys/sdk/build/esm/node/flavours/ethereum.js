import BaseNodeIrys from "../base.js";
import EthereumConfig from "../tokens/ethereum.js";
export class EthereumIrys extends BaseNodeIrys {
    constructor({ url, key, config }) {
        super({
            url,
            config,
            getTokenConfig: (irys) => new EthereumConfig({
                irys,
                name: "ethereum",
                ticker: "ETH",
                providerUrl: config?.providerUrl ?? "https://cloudflare-eth.com/",
                wallet: key,
                opts: config?.tokenOpts,
            }),
        });
    }
}
//# sourceMappingURL=ethereum.js.map